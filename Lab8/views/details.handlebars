<main>
    <div>
        <h1>{{name}}</h1>
        <img alt="{{name}}" src={{image}} />
        <p>{{{description}}}</p>
        <h2>comics</h2>
        <ul>
            {{#each comics}}
                <li>{{name}}</li>
            {{/each}}
        </ul>

    </div>
</main>